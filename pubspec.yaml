name: women_safety_app
description: A Women Safety App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Add these one by one as you need them
  provider: ^6.1.1
  geolocator: ^10.1.0
  shared_preferences: ^2.2.2
  permission_handler: ^11.3.0
  sensors_plus: ^4.0.0
  speech_to_text: ^7.0.0
  url_launcher: ^6.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true